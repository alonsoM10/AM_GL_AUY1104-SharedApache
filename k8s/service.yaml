apiVersion: v1
kind: Service
metadata:
  name: apache-server
  labels:
    app: apache-server
spec:
  type: NodePort
  selector:
    app: apache-server
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30100
      protocol: TCP
